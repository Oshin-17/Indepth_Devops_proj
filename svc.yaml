apiVersion: v1
kind: Service
metadata:
  name: dev_app_service
  namespace: default
spec:
  type: NodePort
  selector:
    app: dev_app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080