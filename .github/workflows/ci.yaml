name: CI Pipeline

on:
  push:
    branches:
    -  main
  # pull_request:
  #   branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    # - name: Set up environment
    #   run: echo "Setting up build environment"
    
    - name: Build
      run: docker build -t dev_app .
    
    # - name: Test
    #   run: echo "Running tests"
    
    # - name: Deploy
    #   run: echo "Deploying application"